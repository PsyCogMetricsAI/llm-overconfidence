# Reference results

These are the study results for 6,701 models from 1,330 uploading organizations.
`REFERENCE_IDENTITIES.json` records their exact file hashes. They are comparison references, not
inputs to model fitting. Recompute from acquired data using the public core pipeline.
The primary support rule is evaluated over all 31 contrasts with simultaneous intervals.
